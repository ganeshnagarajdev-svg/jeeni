# Project Settings
PROJECT_NAME="Jeeni E-commerce"
API_V1_STR="/api/v1"
DEBUG=True

# Database Configuration
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=jeeni_db
POSTGRES_PORT=5432
POSTGRES_POOL_SIZE=20
POSTGRES_MAX_OVERFLOW=10

# Redis Configuration
REDIS_URL=redis://localhost:6379

# Security & Authentication
# CHANGE THIS IN PRODUCTION! Run "openssl rand -hex 32" to generate a secure key.
SECRET_KEY=CHANGE_THIS_TO_A_SECRET_KEY
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Configuration
# Comma-separated list of origins
BACKEND_CORS_ORIGINS=["http://localhost","http://localhost:4200"]

# Allowed Hosts
# Comma-separated list of allowed hosts
ALLOWED_HOSTS=["localhost","127.0.0.1"]
